# PLAN2 — hole.io 고퀄리티화 로드맵 (rev.4)

> **Phase A 완료** (2026-07-29). 구현·검증 결과는 `PLAN.md §25` 에 있다.
> 계획과 달라진 것 셋을 여기 남긴다 — 다음 Phase 가 이 문서를 그대로 믿으면 안 되는 자리다.
>   1. **바다는 지면 메시를 키워 만들지 않았다.** A3 는 PlaneMesh 확대를 적었지만, 주입으로
>      "지면을 키우면 화면에서 하늘이 사라져 H 계열의 배경 기준점이 없어진다" 가 실증됐다.
>      바다는 **지도 안쪽 테두리 한 줄**(W 셀 52개)이고 놀이 영역은 384×384 로 줄었다.
>   2. **강은 남북(열 k=2)이고 교량은 동서 대로 셋이다.** A3 의 "동서 관통" 예시와 축이 다르다 —
>      판정 지점·AI 스폰 링과 겹치지 않는 열을 고른 결과다.
>   3. **Z1 은 휘도가 아니라 채널 우세도의 순서로 판정한다.** 네 지구가 D1·D4 때문에 좁은
>      휘도 띠 안에 들어가야 해서 휘도로는 갈리지 않는다.
>
> Phase A 에서 드러난 **선재 결함 둘**도 함께 고쳤다(§25): 브라우저 게이트의 배경 탐색이
> 창 크기에 따라 뒤집히던 위약, 겹친 구멍 둘이 같은 개체를 각각 삼켜 면적이 두 배로
> 늘던 버그. 후자는 G2 가 잡았다.
>
> 다음은 **Phase C(UI·모바일 입력)** 다.

> 이 문서는 PLAN.md(§1~§24, 기반 시스템 완성)의 **다음 단계** 설계다.
> 작성 세션은 계획·감사까지 수행했고, **구현은 후속 세션(Opus)이 이 문서를 따라 한다.**
> PLAN.md 의 규격·판정·문서 동기화 규율(`tools/sync_plan_blocks.ps1`)은 그대로 유효하다.
> 구현이 끝난 부분은 PLAN.md 에 새 §로 편입하고, 이 문서의 해당 항목에 완료 표시를 남긴다.

---

## 0. 현재 상태 비판적 분석

기반은 튼튼하다 — 결정적 배치(`plan()`, E4), 셰이더 단일면 도로(§1), 물리적 림(§23),
3겹 기계 판정(§24). 그러나 "게임"으로 보면 네 가지가 없다.

| # | 문제 | 구체적 근거 |
|---|------|------------|
| 1 | **도시가 부자연스럽다** | 32m 완전 균질 격자. 유일한 변화가 3배수 대로(§18). 카탈로그 확률이 위치와 무관해 병원·은행·주택이 무작위 혼재. 다중 블록 구조물(공원·오피스 지구) 없음. 수계 없음. 지도 끝은 보이지 않는 clamp — 세계가 뚝 끊긴다. |
| 2 | **배경이 정적이다** | 차량 11종(카탈로그 48항목 중)의 배치가 전부 "주차" 상태(frozen RigidBody). 시민은 에셋 자체가 없다. 신호등은 장식. 움직이는 것은 구멍 6개뿐. |
| 3 | **UI 가 HUD 텍스트뿐** | Label 4개(점수/타이머/보드/종료). 시작 화면·결과 화면·설정 없음. **입력이 키보드(WASD/화살표)뿐** — 웹 배포인데 모바일에서 플레이 불가. R 키 재시작은 안내조차 게임 오버 텍스트에만 있다. |
| 4 | **반복 플레이 유인이 0** | 판 사이에 이어지는 상태가 없다. 저장·해금·기록·목표 전무. |

지켜야 할 자산(회귀 금지):
- 판정 광장(`PLAZA_R = 26`, 원점)과 그 주변 규격 — 1b·2·3b·5 판정 시나리오 전체가 이 위에 서 있다.
- `plan()` 순수성·시드 재현성(E4), 셰이더/판정기 규격 이중 구현 원칙(판정기는 구현체를 읽지 않는다).
- 물리적 림 흡입 모형(§23) — 신규 동적 개체도 같은 규약(레이어 2, swallowable, frozen)을 따른다.
- 한글 HUD 폰트 서브셋(§21) — 새 문구는 폰트 재생성 없이는 화면에서 사라진다.

---

## 1. 설계 원칙 (모든 Phase 공통)

- **P1. 규격은 손으로 저작한 결정적 상수다.** 지구(zone) 지도는 시드 난수가 아니라
  **고정 상수표**로 둔다. 이유 셋: ① 판정기가 독립 구현으로 같은 표를 들 수 있다
  ② 강·공원·다운타운을 의도적으로 디자인할 수 있다 ③ E4 재현성이 자동으로 성립한다.
- **P2. 원점 주변은 불변.** 판정 광장을 포함하는 중앙 2×2 셀은 다운타운(D)으로 고정하고
  도로·보도 규격을 현행 그대로 둔다. 기존 판정의 전제를 건드리지 않는다.
- **P3. 동적 개체는 `judging` 중 기본 비활성 — 단, M 판정은 명시적으로 켠다.**
  기본값: 판정 모드에서는 traffic·citizens 가 아무것도 스폰하지 않는다. 예외: 동적 개체
  자체를 검증하는 M 시리즈 판정(B4)은 **전용 judge 플래그**(예: `--judge7`)로 실행되고,
  그 판정 시나리오가 traffic·citizens 에 스폰을 **명시적으로 요청**한다 — §20 판정
  픽스처("판정에만 존재한다")와 정확히 같은 패턴의 역방향("게임에만 존재하되, 전용
  판정이 부를 때만 판정에도 존재한다")이다. 기존 판정(정적 도시 전제)은 이 플래그를
  쓰지 않으므로 전제가 깨지지 않는다.
- **P4. 웹(gl_compatibility) 성능 예산 안에서.** 데스크톱 3c 프레임 판정 + 브라우저
  rAF 16.7ms 를 지키는 수량으로 시작하고, 수량은 실측으로 조정한다.
- **P5. 신규 기준은 즉시 고장 주입으로 검증한다**(누적 85종 문화 유지). 작업 시작 전
  **baseline 3겹을 먼저 돌린다** — 문서의 "전부 PASS" 는 stale 할 수 있다.
- **P6. 구현 순서는 A → C → B → D.** UI(C)를 교통(B)보다 앞에 두는 이유: 모바일 입력이
  없으면 웹 배포의 절반이 죽어 있고, C 는 B 와 코드가 겹치지 않아 병렬 리스크가 없다.
  각 Phase 끝마다 배포 + 3겹 판정 + PLAN.md 편입까지 하고 다음으로 넘어간다.

---

## 2. Phase A — 도시 규격 v2: 지구제·수계·수퍼블록

**목표**: "격자 위에 그려진 진짜 도시" — 격자 인프라(셰이더 도로·32m PITCH)는 유지하되,
그 위에 존·수계·수퍼블록을 얹어 균질성을 깬다.

### A1. 지구 지도 (`district` 규격)

- 14×14 셀(블록과 1:1) 고정 상수표. 표현은 문자열 14줄 — 사람이 읽고 고칠 수 있다.
  ```
  종류: D 다운타운(고층·오피스)  C 상업  R 주거  P 공원  W 수역
  ```
- 배치 원칙(저작 가이드): 중앙 2×2 = D 고정(P2). D 를 중심으로 C 가 감싸고 외곽은 R.
  P 는 2×2 수퍼블록 1개 + 1×2 1개 이상. W 는 강 1줄이 지도를 동서로 관통(아래 A3).
- 진실 원천은 `city.gd` 의 상수 하나. 셰이더에는 **16×16 데이터 텍스처**(R8, nearest,
  밉맵 없음)로 구워 넘긴다(gl_compatibility 에서 uniform 배열 196개보다 안전).
  판정기는 텍스처를 읽지 않고 **자기 SPEC_ZONES 사본**을 든다(이중 구현 원칙).
- 14×14 는 셀 중심 `(k·32+16, j·32+16)`, 인덱스 `k,j ∈ [-7, 6]` — `block_centers()` 와
  같은 순회 순서로 문자열 행에 대응시킨다. **행·열 → 월드 좌표 대응식을 PLAN.md 에
  명문화**한다(셰이더·GDScript·판정기 세 곳이 같은 식을 들어야 한다).

### A2. 도로 마스크 — 수퍼블록·수역에서 도로를 걷어낸다

- 규격: 도로 세그먼트의 존재는 **존 지도에서 파생되는 순수 함수**다. 별도 데이터를 두지
  않는다. 정의:
  - 어떤 도로 세그먼트(중심선 k, 셀 j 구간)의 **양쪽 셀이 모두 P** 이면 그 세그먼트는
    없다(공원 수퍼블록 내부 관통 도로 제거). 양쪽 중 하나라도 P 가 아니면 존재
    (공원 둘레 도로는 남는다 — 공원은 도로로 둘러싸인 것이 자연스럽다).
  - **어느 한쪽 셀이 W** 이면: 그 세그먼트가 `BRIDGES` 상수 목록(교량, 2~3개, 대로
    라인과 일치)에 있으면 존재, 없으면 없다.
- 셰이더 v2: 프래그먼트가 자기 위치의 양쪽 셀 존을 텍스처에서 샘플해 도로 밴드를
  마스킹한다. 도로가 없는 자리는 존 지면색. **경계 셀(floor 가 픽셀마다 갈라지는 지점)의
  fwidth 튐**은 §18 때와 같은 성질이므로 같은 논거로 무해함을 확인하고 기록한다.
- 판정기 갱신: 블록 지면 표본(`|u| = CURB_HALF + 1`)·대로 표본(`boul_far`)이 **P·W 셀에
  떨어지면 표본을 옮기거나 그 셀을 건너뛴다**. "어떤 표본이 어떤 존 전제를 갖는가" 를
  판정기 쪽 SPEC 으로 명문화한다.

### A3. 수계 — 강·바다·교량

> **구현은 이 절과 다르다(§25가 최종 규격이다).** 강은 **남북(열 k=2)** 이고 교량은
> 동서 대로 셋(`BRIDGES = [[-3,2],[0,2],[3,2]]`)이며, 바다는 지면 메시 확대가 아니라
> **지도 안쪽 테두리 한 줄**이다. Phase B 가 M3 의 교량 예외를 적을 때 이 절이 아니라
> §25 를 봐야 한다.

- **강**: W 셀 1줄이 지도를 동서로 관통(행 선정은 아래 "강 행 선정 제약" 참조).
  셰이더가 물색 + 기슭 밴드를 그린다. 움직이는 물결은 P2 스트레치 — 넣는 경우
  **판정 모드에서는 time uniform 을 고정**한다("표본과 무관한 위치" 같은 공간 제한은
  셰이더로 표현하기 어색하다 — 시간을 멈추는 쪽이 규격으로 깨끗하다). 강 위 교량
  2~3개는 대로 세그먼트로 남는다(A2).
- **바다**: `|p.x| > GROUND_HALF or |p.z| > GROUND_HALF` 인 픽셀은 물색 — PlaneMesh 를
  키우고 셰이더가 경계 밖을 바다로 칠한다. "세계가 뚝 끊기는" 문제의 최소 비용 해소다.
  **크기는 실측으로 정한다**: 지도 네 모서리에 구멍을 두고 카메라 프러스텀이 닿는
  최원거리를 재서 + 마진. 기본 시작값은 반폭 1024(평면 하나라 비용이 없다 — 인색할
  이유가 없다). 물리 지면 콜라이더는 현행 유지(어차피 구멍이 못 나간다).
- **구멍 이동 제한**: `move_to()` 가 목표 위치의 존을 조회해 W 셀이면 **교량 도로 폭
  안이 아닌 한** 축별로 clamp 한다(기존 지면 경계 clamp 와 같은 슬라이드 방식 —
  강가를 따라 미끄러진다). 존 조회 함수는 `city.gd` 의 static 함수로 두고 hole·AI 가
  공유한다. AI 의 `pick_wander()`·목표 선정도 W 셀 목표를 버린다.
  - **교량 위 구멍**: 교량은 시각적으로 데크를 올리지 않는다 — 노면과 같은 높이의
    "둑길(causeway)" 로 그린다. 구멍이 그대로 지나갈 수 있고 우물 착시도 깨지지 않는다.
    데크·난간을 올리는 순간 구멍 통과·림 물리·H 판정이 전부 복잡해진다(명시적 비채택).
  - **기슭에 걸친 구멍**: Z5 는 구멍 **중심** 기준이므로 원반의 절반이 물 위에 걸칠 수
    있고, 지면 셰이더의 구멍 SDF 는 물색 픽셀도 뚫는다. 이것은 **허용으로 결정한다** —
    "강기슭이 구멍에 파인" 연출로 자연스럽고, 반경 마진으로 clamp 하면 교량 진입로
    접근까지 막힌다. 구현자가 이를 버그로 오인해 고치지 말 것.
  - **AI 와 강**: `hole_ai.gd` 의 `pick_wander()` 는 전역에서 지점을 뽑고 **도달해야만**
    재추첨한다(현행 `to.length() < 0.05`). 강 건너 배회 지점을 향하던 AI 는 clamp 슬라이드
    때문에 둑을 영원히 밀며 정지한다 — 목표 셀이 W 인지 검사하는 것으로는 부족하다
    (문제는 목표가 아니라 **경로**다). 완화 규격: **N 프레임(예: 120) 동안 실이동이
    0.5m 미만이면 ① 배회 지점을 재추첨하고 ② 현재 추적 목표를 일정 시간(예: 600
    프레임) 블랙리스트에 넣어 목표 선정에서 제외**한다 — 배회만 재추첨하면
    `choose_target()` 이 강 건너 최근접 먹이를 계속 goal 로 골라(강폭 32m < sight 70m)
    같은 정지가 목표 추적 경로로 재발한다. 경로 탐색은 도입하지 않는다(명시적 비채택 —
    이 게임의 AI 는 조언 수준이면 충분하다). G 시리즈 자유 실행 판정(AI 최소 이동량)이
    이 완화로 계속 성립하는지 baseline 대조를 A 수용 기준에 포함한다.
  - **강 행 선정 제약**: AI 스폰은 반경 128 원을 격자 교차로로 스냅하며 `move_to()` 를
    거치지 않는다 — 강 행을 스폰 링이 지나는 자리(예: (±32, ±128) 부근)에 두면 AI 가
    W 셀 경계선 위에서 태어나 Z5 불변이 스폰 순간부터 깨진다. **강 행은 스폰 링이
    닿지 않는 행으로 고르거나, 스폰 스냅에 W 회피를 추가**한다(전자가 싸다).

### A4. 존별 배치 규격 (`plan()` v2)

- 카탈로그 각 항목에 `zones`(허용 존)와 존별 `weight`·`scale_mul` 을 더한다:
  - **D**: Building*_Large·Bank·Flat 계열, scale_mul 1.3~1.6(고층감), 중앙 슬롯 확률 0.45→0.8,
    산포 밀도 ↑. 나무·주택 배제.
  - **C**: Shop·Flat·Building*_Small, 가로 시설물 밀도 ↑.
  - **R**: House 계열 + 나무·덤불, 밀도 ↓(마당 여백).
  - **P**: 나무·바위·덤불만, 밀도는 R 과 비슷.
  - **W**: 아무것도 없음(in_zone 이 존 조회로 자동 거절).
- **P 수퍼블록의 배치 기하 — 셀 병합 규격 (핵심, 선언만으로는 부족하다)**:
  셰이더에서 내부 도로를 지워도(A2) `block_span()`·`in_zone()` 의 "block" 분기는 셀
  단위로 커브 경계를 물고 있어, 그대로 두면 프롭이 옛 도로 띠를 계속 피해 "도로만
  지워진 빈 십자 띠" 가 남는다 — 수퍼블록이 아니라 블록 4개다. 규격:
  - `block_span()` 을 존 인지로 확장한다: 내 셀이 P 이면 같은 축의 이웃 셀이 P 인 동안
    구간을 이어 붙이고, **첫 비-P 인접 중심선의 커브**에서 멈춘다(제거된 내부 도로
    자리는 공원 지면이므로 구간에 포함된다 — 옛 도로 띠 위에도 프롭이 놓인다).
  - 배치 시행은 **수퍼블록의 대표 셀(k·j 최소인 셀)에서 한 번만** 돌린다. 나머지 P
    셀은 건너뛴다 — 셀마다 돌리면 같은 병합 구간에 밀도가 셀 수 배로 중복된다.
    산포 반경은 병합 구간에서 유도(§22 의 구간 유도 로직 그대로, SPREAD_MAX 는 병합
    구간에 맞게 완화).
  - `in_zone()` "block" 분기도 같은 병합 span 을 쓴다(두 함수가 같은 진실을 들어야
    거절이 일관된다 — §22 와 같은 논거).
  - 대표 셀은 **(k, j) 사전순 최소**(k 가 최소인 것들 중 j 최소)로 확정한다.
- **정적 배치도 A2 마스크를 따른다**: `plan_walk`·`plan_road` 는 **A2 도로 마스크를
  통과한 세그먼트에만** 시행한다(B1 차량·B2 시민과 같은 파생). 이것이 없으면 P
  수퍼블록 내부의 제거된 도로 자리 보도 띠에 가로등이 서고, 강기슭의 제거된 도로
  차선 오프셋에 주차 차량이 맨땅 위에 선다 — `in_zone` 의 walk/road 분기는 기하
  띠만 재므로 자동으로 거절되지 않는다. Z2 의 주입 항목에 "제거 세그먼트 위
  walk/road 프롭 0" 검사를 추가한다.
- 스케일 배수를 바꾸면 **질량·top_height·fit_radius 가 전부 따라 변한다** — swallowable
  파이프라인이 이미 AABB 파생이므로 코드 변경은 없지만, D 존 대형 건물이 후반 구멍만
  삼킬 수 있는 크기가 되는지 실측 확인 항목으로 둔다(진행 곡선의 목적).
- `plan()` 의 난수 소비 흐름이 전부 바뀐다 — E4 는 자기 일관성 판정이라 통과하지만,
  PLAN.md 의 §22 계측 수치(블록당 프롭 등)는 무효가 된다. **해당 §에 "v2 에서 재계측"
  주석을 달고 새 계측을 기록**한다.

### A5. Phase A 수용 기준 (기계 판정 + 휴먼 검수)

기계(신규 판정, 각각 고장 주입 1회 이상):
- **Z1 존-지면색**: 존 대표 셀(각 존 1개 이상, 판정기 SPEC 좌표) 지면 휘도가 존별 기대
  구간에 든다. 주입: 존 텍스처를 한 칸 밀어 넣으면 탈락해야 한다.
  **존 팔레트 저작 제약**: 기존 판정 D1/D4 는 블록 지면과 커브(0.72,0.72,0.70)·도로의
  휘도차 하한을 요구한다. 어느 존의 지면색도 이 하한을 깨면 안 된다 — 특히 D 존을
  콘크리트 톤으로 잡을 때 커브와의 휘도차를 확인하고, 팔레트 확정 시 D1/D4 를 먼저
  돌려 본다. **D3(블록 간 동일 표면 휘도 편차)는 존별 지면색과 정면 충돌한다** —
  판정기를 "**같은 존의 블록끼리만** 비교" 로 개정한다(재유도 절차의 일부).
- **Z2 공원 무도로**: P 수퍼블록 내부 관통선 위 표본에 도로 휘도가 없다. 주입: 도로
  마스크 함수를 끄면 탈락.
- **Z3 수역 무프롭**: `plan()` 결과에 W 셀 내부 프롭 0개. 주입: in_zone 존 검사 제거.
- **Z4 교량 존재**: BRIDGES 세그먼트 위 표본에 아스팔트 휘도. 주입: 교량 목록 비우기.
- **Z5 수역 통행 불가**: `move_to()` 로 강 중앙을 목표로 줘도 구멍 중심이 W 셀에 못
  들어간다 + 교량 위로는 들어간다. 주입: move_to 존 검사 제거.
- **기존 3겹은 "재유도 후 PASS"다 — 그대로는 PASS 가 아니다.** 판정기에는 구현
  계측에서 유도된 상수들이 박혀 있다(예: 재시작 후 프롭 총수 정확 일치, 대로 위
  차량·보도 프롭 최소 수, 수관 프롭 최소 수 등 — `screenshot.gd` 의 계측 파생 상수
  일체). `plan()` v2 는 프롭 총수·분포를 반드시 바꾸므로 이 상수들은 **v2 배치에서
  재계측해 재유도하고, 갱신한 상수마다 고장 주입을 다시 건다**. 구현자가 T5/T7 FAIL
  을 만나고서야 "판정기를 고쳐도 되는가" 를 즉석 판단하게 두지 않는다 — 재유도는
  이 계획이 승인한 절차다. 재유도 후 3겹 전부 PASS 가 수용 기준이다.
계측(P4): 셰이더 v2 적용 후 3c 프레임 판정 통과(텍스처 샘플 추가 비용 확인).
휴먼 검수: 강·공원·다운타운·바다 경계가 항공 뷰 스크린샷에서 육안 판별되는가,
"도시답게" 보이는가(아트 판단은 사람 — 전역 원칙 §1).

---

## 3. Phase C — 게임 UI (교통보다 먼저)

**목표**: 시작→플레이→결과→재시작의 완결된 흐름 + 모바일 입력. hole.io 톤의 심플 캐주얼 UI.

### C1. 화면 흐름

```
[시작 화면] 타이틀 / 시작 버튼 / 스킨 선택(Phase D 연동) / 조작 안내
     ↓ 시작
[인게임 HUD] (아래 C2)
     ↓ 종료(시간/먹힘)
[결과 화면] 순위표 / 내 점수·신기록 배지 / 통계(파괴율 등, Phase D 연동)
            / 다시하기 / 홈
```
- 구현: `main.tscn` 위 CanvasLayer 씬 3개(Start/HUD/Result) + `ui.gd` 상태 머신.
  기존 `restart()` 를 재사용한다 — 씬 reload 금지 이유(main.gd `restart()` 주석:
  Judge 노드가 Main 의 자식이라 reload 하면 함께 사라진다)가 그대로 유효하다.
- **판정 호환**: 판정 모드(`judging`)에서는 시작 화면을 건너뛰고 바로 PLAYING 으로
  들어간다(기존 판정이 "부팅 즉시 플레이 상태" 를 전제한다). 게임 모드만 시작 화면.

### C2. 인게임 HUD 개선

- 좌상: 점수 + **크기 레벨 진행 바**(반경 구간을 레벨 1~10 으로 나눠 다음 레벨까지 %).
  hole.io 의 핵심 피드백 장치다.
- 우상: 타이머(현행), 순위 패널은 상위 3 + 나(전체 6줄 → 압축).
- **킬 피드**: 구멍이 구멍을 먹으면 우측에 "AI2 → AI5" 3초 표시.
  주의: "→"(U+2192) 등 **기호는 현재 폰트 서브셋(ASCII + 한글 음절 26자) 밖**이다.
  동적으로만 뜨는 문구는 T8 정적 검사를 빠져나갈 수 있으므로, C4 일람표에 기호까지
  포함해 서브셋에 넣거나 ASCII 대체("AI2 -> AI5")를 쓴다.
- 성장 순간 "+n" 팝업(구멍 화면 위치에 1초 떠오름) — `swallowed` 시그널 재사용.
- 전부 기존 Label/ProgressBar/Theme 로 — 이미지 에셋을 만들지 않는다(아트 감사 범위
  최소화. 필요한 것은 색·둥근 모서리 StyleBoxFlat 로 충분).

### C3. 입력 — 모바일·마우스 (가장 급한 결함)

- **가상 조이스틱**: 터치 시작점 기준 드래그 벡터 → `move_hole` 방향. 화면 아무 데나
  터치(플로팅). 반투명 원 2개(베이스+노브) 표시.
- **마우스 드래그**: 좌클릭 홀드 중 커서-구멍 화면 벡터로 이동(데스크톱 웹 유저).
- 키보드는 현행 유지. 셋 다 `move_hole()` 하나로 수렴 — 입력원별 분기는 방향 벡터
  산출까지만.
- InputMap 정식화(§14 의 "2단계에서 정식화한다" 미이행 항목을 여기서 갚는다).

### C4. 폰트·문구 규율 (§21 연동)

- **UI 문구 전체를 먼저 확정**하고(시작/결과/설정/업적 문구 일람표를 PLAN.md 에 적는다),
  `assets/fonts/README.md` 절차로 서브셋을 **한 번에** 재생성한다. 찔끔찔끔 추가 금지 —
  음절 하나 빠지면 그 글자만 소리 없이 사라진다. 일람표는 **음절뿐 아니라 기호·숫자
  포맷 문자까지** 포함한다(킬 피드 화살표 등 동적 문구 포함 — C2 주의 참조).
- T8 판정의 `SPEC_HUD_CHARS` 를 새 문구 전체로 갱신 + 고장 주입(문구에 서브셋 밖 음절
  하나 추가 → 탈락 확인).

### C5. Phase C 수용 기준

- 기계: T8 확장 PASS(주입 포함) · 판정 모드가 시작 화면 없이 기존과 동일하게 도는지
  3겹 전부 PASS · UI 상태 머신 판정 1개 신설(U1: 시작→플레이→종료→재시작 전이가
  코드 경로로 재현되고 각 상태에서 보이는 CanvasLayer 가 규격과 일치. 주입: 전이 하나
  제거 → 탈락).
- 휴먼: 모바일 실기(또는 Chrome 터치 에뮬)로 시작→플레이→결과→다시하기 완주.

---

## 4. Phase B — 동적 배경: 교통·시민

**목표**: 도시가 살아 움직인다. 차가 다니고 시민이 걷고, 구멍이 다가오면 도망친다.
전부 먹이다(swallowable 규약 유지).

### B1. 교통 시스템 (`traffic.gd`)

- **차선 그래프는 규격에서 파생**한다: 도로 세그먼트(A2 마스크 통과분) × 우측통행
  주행 오프셋. 별도 데이터 없음. **주의: 현행 `lane_slots()` 는 "주행 띠" 가 아니라
  정차 자리 그 자체다** — `plan_road()` 가 바로 그 오프셋에 차를 세운다. 주행 차선을
  같은 오프셋에 돌리면 frozen 차량 사이에는 충돌 해소가 없어 주행차가 주차차를
  그대로 관통한다. 규격을 분리한다:
  - **주행 오프셋**(신규): 일반 도로 ±2.0(전 폭이 주행), 대로는 안쪽 자리
    ±(median + w·0.25) 부근만 주행 차선.
  - **주차 오프셋**: 일반 도로에서는 노상 주차를 없애고(폭 8m 에 주행·주차 공존 불가),
    대로 바깥 자리 ±(median + w·0.75) 만 주차 전용으로 남긴다 — `plan_road()`/
    `lane_slots()` 를 이 규격으로 개정한다("밀도를 낮춰 유지" 의 구체형).
  - 주행 차선 위의 잔존 정지물(트래픽콘 등)은 **1D 추종의 "정지 차량"** 으로 취급한다
    — 주행차는 그 뒤에 선다. 스폰 시에도 기존 프롭과의 중첩을 회피한다(`fits()` 재사용).
  - **대로 가운데 자리(반extent 최대 2.725)의 대형 주차 차량은 주행 차선(안쪽 자리)을
    덮는다** — 그대로 두면 그 차선이 영구 폐색되어 뒤에 선 차들이 그리드락의 씨앗이
    된다. 대로 가운데 정차 자리는 **폐지하거나 주행 차선을 덮지 않는 오프셋으로
    옮긴다**(plan_road 개정에 포함). 어느 쪽이든 대형 차량(버스·구급차)의 정차 자리는
    바깥 자리에 유지한다 — "가장 넓은 도로가 가장 큰 차를 거절한다" 회귀 금지(§18).
- 차량 40대(초기값, P4 실측으로 조정)를 시드 고정으로 도로 위에 스폰. **frozen
  RigidBody + swallowable** 그대로 두고, 트래픽 컨트롤러가 매 물리 프레임
  `global_position` 을 전진시킨다(§23 규약과 충돌하지 않는다 — frozen 은 성능 장치일
  뿐이고 이동 주체가 컨트롤러일 뿐).
- 주행 규칙(단순 유지 — 신호 연동·차선 변경 없음, 명시적 비채택):
  - 세그먼트 직진, 교차로에서 시드 난수로 직/좌/우(없는 세그먼트는 후보 제외 — 교량
    건너기·공원 우회가 자동으로 나온다).
  - 전방 동일 차선 차량과 최소 간격 미달 시 정지(1D 추종). 데드락(서로 기다림)은
    교차로 진입을 한 대씩만 허용하는 타일 점유 플래그로 회피하되, **진입은 출구
    세그먼트에 빈 자리가 있을 때만 허용**한다 — 진입한 차가 앞이 막혀 교차로를 영구
    점유하면 막힘이 교차 방향으로 전파되는 그리드락이 된다(영구 정지 차량이 있는
    도시에서는 반드시 생기는 모드다).
  - **구멍 감지**: 자기 위치가 어느 구멍의 rim 감지 범위에 들어가면(`enter_rim` 호출을
    받으면) 컨트롤에서 제외하고 물리에 인계한다. 인계 시 주행 속도를
    `linear_velocity` 로 넣어 관성이 이어지게 한다(프레임 튐 방지).
  - 먹힌 수만큼 지도 가장자리 도로에서 재스폰(총량 유지, 시드 이어서).
- 주차 차량(현행 `plan_road`)은 밀도를 낮춰 유지 — 길가 주차는 실제감에 기여한다.
- `judging` 이면 traffic 노드가 아무것도 스폰하지 않는다(P3).

### B2. 시민 (`citizens.gd`)

- **에셋**: 저장소에 인간 모델이 없다. **절차 생성 미니피규어**를 채택한다 — 캡슐
  몸통 + 구 머리(2 메시, 존별 옷색 팔레트), 코드 생성이라 외부 에셋 조달·라이선스·
  임포트 파이프라인이 전부 불필요하다. 걷기 애니메이션 대신 **이동 방향 기울임 +
  상하 밥(bob)** — 카메라 고도에서 충분히 "걷는 사람" 으로 읽힌다.
  - 아트 감사 기준(전역 §1): 치명 결함(0픽셀·지면 관통·크기 오류)만 기계로 잡고
    미감은 휴먼 검수.
- **행동**: 보도 중심선(`walk_center_at`) 위 웨이포인트 순찰 — 웨이포인트는 **A2
  도로 마스크를 통과한 세그먼트의 보도만** 쓴다(제거된 도로의 보도·W 셀 위를 걷지
  않는다. 차량의 세그먼트 규칙과 같은 파생). 구멍이 반경 ×3 안에 들어오면 반대
  방향으로 가속 도주(hole.io 의 재미 포인트). rim 진입 시 물리 인계는 차량과 동일.
- **수량·LOD**: 120명 시드 스폰. 카메라 시야 밖(구멍 기준 거리 > 70m)은 4프레임에
  1회 갱신. 먹히면 재스폰(총량 유지).
- swallowable 값: fit_radius 소(0.3 내외) — 시작 반경 1.5 에서 즉시 먹이. score 소.
- HUD 문구에 새 음절이 필요하면 C4 일람표에 선반영.

### B3. 앰비언트 (P2 스트레치 — 이번 범위에서 명시적 제외 가능)

- 신호등 점멸(이미시브 색 교대, 교통 로직과 비연동 장식), 배경 새 그림자, 물결 반짝임.
- 사운드(흡입·성장·BGM)는 별도 Phase 로 뺀다 — 에셋 조달·볼륨 설정 UI 가 얽혀
  이번 4개 축과 독립적이다. Phase D 뒤 후보로 기록만 한다.

### B4. Phase B 수용 기준

- 기계(각각 고장 주입). **M1~M3·M5 는 P3 의 예외 규정대로 전용 judge 플래그로 돌며,
  그 시나리오가 traffic·citizens 스폰을 명시적으로 요청한다**:
  - **M1 이동 실증**: 자유 실행 300 물리 프레임에서 차량 평균 변위 > 20m, 시민 > 3m.
    주입: 컨트롤러 틱 끄기 → 탈락.
  - **M2 결정성**: 같은 시드 2회 실행의 300프레임 후 전 차량·시민 위치 지문 일치.
    주입: 시드에 프레임 시각 섞기 → 탈락.
  - **M3 규격 준수**: 임의 표본 시점에 모든 주행 차량 중심이 주행 차선 규격 안
    (**교차로 타일 안에서 회전 중인 차량은 예외** — 어느 차선 띠에도 속하지 않는
    것이 정상이다) + 제거된 세그먼트 위 차량·시민 0 + W 셀 위 차량·시민 0
    (**단 BRIDGES 세그먼트 위는 제외** — 교량 통행은 B1 이 규정한 정상 동작이다).
    주입: 도로 마스크 무시 → 탈락.
  - **M4 판정 격리**: 전용 플래그 **없는** 판정 실행에서 동적 개체 0. 주입: P3 기본
    가드 제거 → 탈락.
  - **M5 인계 무결성**: 차량이 rim 진입 → 낙하 → 소멸 → 점수 반영이 기존 C 시리즈와
    동일 규약으로 성립(§23 판정 재사용).
- 계측: 데스크톱 3c 에 "교통 on" 시나리오 추가(브라우저는 §24 한계대로 계측만).
  프레임 예산 초과 시 수량(40/120)을 줄이는 것이 첫 번째 손잡이다.
- **재유도 재적용**: B1 의 `plan_road`/`lane_slots` 개정도 판정기의 계측 파생 상수를
  다시 깬다 — A5 의 "재유도 후 PASS" 절차를 Phase B 에서도 그대로 밟는다.
- 휴먼: 차가 교차로에서 자연스럽게 갈라지는가, 시민 도주가 우스꽝스럽지 않은가.

---

## 5. Phase D — 리텐션: 반복 플레이 유인

### D0. 브레인스토밍 전체 목록 (채택/보류 판단 포함)

| 아이디어 | 판단 | 이유 |
|---|---|---|
| 로컬 기록·통계 영속화 | **채택 D1** | 모든 리텐션의 기반. 비용 소. |
| 스킨 해금 | **채택 D2** | hole.io 검증된 핵심 루프. 셰이더 불변으로 구현 가능. |
| 오늘의 도시(데일리 시드) | **채택 D3** | "매일 한 판" 루프. 비용 소. |
| 결과 통계(도시 파괴율 등) | **채택 D4** | "한 판 더" 트리거. 비용 소. |
| 업적 | **채택 D5(P1)** | 중기 목표 제공. 문구가 폰트 서브셋과 얽혀 C4 선반영 필요. |
| 글로벌 리더보드 | **보류(스트레치)** | Vercel KV+서버리스 필요 — 부정 방지 없는 공개 보드는 즉시 오염된다. 로컬 우선. |
| 게임 모드(무한/솔로 청소) | 보류 | 판정 매트릭스가 모드 수만큼 곱으로 는다. 코어 품질 우선. |
| 콤보/배수 시스템 | 보류 | 성장 곡선(growth_k) 재조정을 강제 — §23 체감 조정과 함께 별도 사이클로. |
| XP/시즌 패스류 | 기각 | 이 규모의 싱글 웹 게임에 과함. |

### D1. 로컬 영속화

- `user://save.json` — 최고 점수, 누적 점수, 누적 삼킴, 플레이 횟수, 해금 스킨,
  오늘의 도시 기록(날짜 키). 웹 익스포트의 `user://` 는 IndexedDB 다 —
  **저장 직후 sync 가 자동이 아닐 수 있으므로**, 저장 시점(판 종료·해금)에 명시적
  플러시를 확인하고 "새로고침 후 유지" 를 판정으로 잡는다(아래 D6-S1).
- 스키마에 `version` 필드 — 이후 필드 추가 시 마이그레이션 지점.

### D2. 스킨 해금 (6종)

- **우물(well) 메시 머티리얼 색만 바꾼다.** 지면 셰이더의 `holes` uniform 구조는
  건드리지 않는다 — H 시리즈 판정(마젠타 탐침 등)의 전제를 보존하는 안전선이다.
  well 메시 머티리얼은 구멍 로컬이라 판정 표면적이 없다. 주의: **림은 물리 전용
  셰이프이고 그려지지 않는다**(hole.gd `rim_shape`) — 스킨을 위해 림 시각 메시를
  신설하지 말 것(H 판정 표면적이 늘어난다).
- 해금 조건: 누적 점수 구간(예: 0 / 5k / 20k / 50k / 150k / 400k). 시작 화면에서 선택.
- 판정 모드는 항상 기본 스킨(판정 스크린샷 휘도 전제 보호).

### D3. 오늘의 도시

- 게임 모드의 `city_seed` 를 날짜(로컬 자정 기준 YYYYMMDD)로 파생. 지구 지도는
  고정 상수(P1)이므로 **바뀌는 것은 프롭 배치뿐** — 판정 표본 전제와 충돌하지 않는다.
  시작 화면에 "오늘의 도시 최고 기록" 표시.
- **판정 모드는 항상 기본 시드**(§20 픽스처와 같은 원칙 — 판정은 날짜와 무관해야 한다).

### D4. 결과 통계

- 도시 파괴율(%): 삼킨 프롭 수 / `plan()` 총 프롭 수(진실 원천은 plan 결과 크기).
- 최다 포식 카테고리(차량/건물/시민/나무), 최대 한 입(최고 score_value), 신기록 배지.

### D5. 업적 (P1 — D1~D4 뒤에)

- 8종 내외: 첫 버스 / 구멍 3개 흡수 / 파괴율 50% / 시민 100명 / 다리 밑 강 구경(교량
  셀 방문) 등. 문구는 C4 일람표에 선반영. 로컬 저장(D1 스키마).

### D6. Phase D 수용 기준

- **S1 영속 왕복**: 저장 → 강제 리로드(웹: 페이지 새로고침) → 로드 값 일치.
  브라우저 게이트에 1종 추가. 주입: 플러시 제거 → 탈락.
- **S2 해금 단조성**: 누적 점수 주입 시 해금 집합이 규격 구간과 일치. 주입: 경계값
  하나 어긋내기 → 탈락.
- **S3 판정 격리**: 판정 모드에서 시드·스킨이 항상 기본값. 주입: 격리 가드 제거 → 탈락.
- 휴먼: 결과 화면에서 "한 판 더" 를 누르고 싶은가(정성 — 유저 보고).

---

## 6. 검증·문서 규율 (전 Phase 공통 절차)

1. **시작 전 baseline**: 데스크톱 Forward+ 9종 · Compatibility 9종 · 브라우저 게이트
   8종을 그대로 돌려 stale PASS 를 걸러낸다.
2. Phase 마다: 신규 판정 구현 → **고장 주입으로 판정 자체를 검증** → 3겹 전체 PASS →
   `pwsh tools/sync_plan_blocks.ps1 -Fix` → 무인자 재실행으로 "어긋난 블록 0개" 확인 →
   PLAN.md 에 새 § 편입 → commit/push → `build/` 에서 Vercel 배포 →
   `index.pck` Content-Length 대조.
3. 판정기는 구현체에서 값을 읽지 않는다 — 신규 규격(SPEC_ZONES·BRIDGES·차선 파생식)은
   판정기 쪽에 **사본**으로 든다. 사본 불일치는 그 자체가 탈락이다.
4. §24 한계(Chrome 하나·기계 하나) 는 이번에도 확장하지 않는다 — 범위 밖으로 명시.

## 7. 리스크와 완화

| 리스크 | 완화 |
|---|---|
| gl_compatibility 에서 존 텍스처 정밀도/샘플 문제 | R8+nearest+밉맵 없음. 실패 시 fallback: 셰이더 내 상수 배열(GLSL const float[196]) — 텍스처 없이 컴파일 타임 상수로. |
| `plan()` 난수 흐름 전면 변경으로 기존 계측 수치 무효 | E4 는 자기 일관성이라 통과. §22 계측은 "v2 재계측" 으로 갱신(A4). |
| 판정기의 계측 파생 상수(프롭 총수 정확 일치 등)가 v2 배치에서 전부 FAIL | A5 의 "재유도 후 PASS" 절차 — 상수 재계측 + 고장 주입 재설치를 계획이 명시적으로 승인. |
| AI 구멍이 강둑에 끼여 정지(배회 지점이 강 건너) | A3 의 무진전 재추첨 규격(120프레임 실이동 < 0.5m). G 시리즈 baseline 대조 포함. |
| 주행 차량이 frozen 주차 차량을 관통 | B1 의 주행/주차 오프셋 분리 + 정지 차량 1D 추종 + 스폰 중첩 회피. |
| 교통 물리 인계 순간 프레임 튐 | 인계 시 linear_velocity 주입(B1). M5 로 판정. |
| 절차 생성 시민이 "에러 없이 0픽셀" | 치명 결함 체크리스트(전역 §1)를 기계 판정으로: 생성 직후 스크린샷에서 실루엣 픽셀 수 > 0 확인. |
| 폰트 서브셋 누락으로 글자 증발 | C4: 문구 일람 선확정 → 일괄 재생성 → T8 확장 + 주입. |
| 웹 프레임 예산 초과(동적 개체 추가) | 수량(40/120)이 첫 손잡이. 데스크톱 3c 시나리오로 회귀 감시, 브라우저는 계측 기록. |
| 판정 광장·기존 시나리오 회귀 | P2(중앙 D 고정) + 매 Phase 3겹 전체 재실행. |
| UI 상태 머신이 판정 부팅 경로를 바꿈 | C1: judging 이면 시작 화면 스킵. U1 판정 + 3겹으로 감시. |

## 8. 규모 감각 (구현 세션 참고용)

| Phase | 신규/수정 주요 파일 | 신규 판정 | 상대 규모 |
|---|---|---|---|
| A 도시 v2 | city.gd, ground_hole.gdshader, hole.gd(move_to), hole_ai.gd, screenshot.gd | Z1~Z5 | 대 |
| C UI | ui.gd+씬 3, main.gd, InputMap, 폰트 재생성 | U1, T8 확장 | 중 |
| B 동적 | traffic.gd, citizens.gd, main.tscn | M1~M5, 3c 시나리오 | 대 |
| D 리텐션 | save.gd, ui 연동, hole 머티리얼 | S1~S3 | 중 |

각 Phase 는 독립 배포 가능한 단위다. A 가 가장 크고 가장 먼저다 — 도시가 곧 게임의
무대이자 먹이 전체이고, B·D 의 규격(차선 그래프·파괴율 분모)이 A 의 산출물에 서 있다.
